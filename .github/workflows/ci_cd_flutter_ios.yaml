name: Flutter iOS CI/CD

on:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: macos-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v3

    - name: Install Flutter
      uses: subosito/flutter-action@v2
      with:
        flutter-version: '3.10.6'

    - name: Install dependencies
      run: flutter pub get

    - name: Install the Apple certificate and provisioning profile
      env:
        P12_PASSWORD: ${{ secrets.CERTIFICATE_PASSWORD }}
        BUILD_PROVISION_PROFILE_BASE64: ${{ secrets.PROVISIONING_PROFILE }}
        KEYCHAIN_PASSWORD: ${{ secrets.KEYCHAIN_PASSWORD }}
        DISTRIBUTION_CERTIFICATE: ${{ secrets.DISTRIBUTION_CERTIFICATE }}
      run: |
        DIS_CERTIFICATE_PATH=$RUNNER_TEMP/dis_build_certificate.p12
        PP_PATH=$RUNNER_TEMP/build_pp.mobileprovision
        KEYCHAIN_PATH=$RUNNER_TEMP/app-signing.keychain-db
        
        echo -n "$BUILD_PROVISION_PROFILE_BASE64" | base64 --decode --output $PP_PATH
        echo -n "$DISTRIBUTION_CERTIFICATE" | base64 --decode --output $DIS_CERTIFICATE_PATH
        
        security create-keychain -p "$KEYCHAIN_PASSWORD" $KEYCHAIN_PATH
        security set-keychain-settings -lut 21600 $KEYCHAIN_PATH
        security unlock-keychain -p "$KEYCHAIN_PASSWORD" $KEYCHAIN_PATH
        
        security import $DIS_CERTIFICATE_PATH -P "$P12_PASSWORD" -A -t cert -f pkcs12 -k $KEYCHAIN_PATH
        security list-keychain -d user -s $KEYCHAIN_PATH
        
        mkdir -p ~/Library/MobileDevice/Provisioning\ Profiles
        cp $PP_PATH ~/Library/MobileDevice/Provisioning\ Profiles

    - name: Run Flutter tests
      run: flutter test

    - name: Build iOS
      run: flutter build ios --release

    - name: Export Xcode Archive
      run: |
        xcodebuild -workspace ios/Runner.xcworkspace \
                   -scheme Runner \
                   -sdk iphoneos \
                   -configuration Release \
                   archive \
                   -archivePath $PWD/build/Runner.xcarchive \
                   DEVELOPMENT_TEAM=${{ secrets.DEVELOPMENT_TEAM }} \
                   -allowProvisioningUpdates

    - name: Export IPA
      run: |
        xcodebuild -exportArchive -archivePath $PWD/build/Runner.xcarchive \
                   -exportOptionsPlist ios/Flutter/ExportOptions.plist \
                   -exportPath $PWD/build/ios_release

    - name: Get AuthKey file
      env:
        APP_STORE_AUTH_P8_KEY: ${{ secrets.AUTH_P8_KEY }}
      run: | 
        mkdir -p ~/private_keys
        echo -n "$APP_STORE_AUTH_P8_KEY" | base64 --decode --output ~/private_keys/AuthKey_53CY77L5AS.p8

    - name: List all Files
      run: |
        ls -l ~/private_keys/
        cat ~/private_keys/AuthKey_53CY77L5AS.p8

    - name: Upload IPA to App Store Connect
      env:
        APP_STORE_CONNECT_KEY_ID: ${{ secrets.APP_STORE_CONNECT_API_KEY_ID }}
        APP_STORE_CONNECT_ISSUER_ID: ${{ secrets.APP_STORE_CONNECT_API_ISSUER_ID }}
      run: |
        xcrun altool --upload-app --type ios \
                     --file $PWD/build/ios_release/Runner.ipa \
                     --apiKey ${APP_STORE_CONNECT_KEY_ID} \
                     --apiIssuer ${APP_STORE_CONNECT_ISSUER_ID} \
                     --apiKeyPath ~/private_keys/AuthKey_53CY77L5AS.p8

    - name: Upload artifact
      uses: actions/upload-artifact@v3
      with:
        name: ios-release
        path: $PWD/build/ios_release
